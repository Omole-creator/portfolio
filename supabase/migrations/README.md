# Migrations

There is no Supabase CLI wired into this repo, so nothing here runs
automatically. These files are a version-controlled record of the SQL that
has been applied by hand in the Supabase SQL editor for project
`adwbbllkbbyqcqjrhpsr`. Apply them in order, and if the CLI is ever adopted,
they already follow its `NNNN_description.sql` naming.
